# Warehouse OS History

Historical release and build notes are archived here so the production repository root stays focused on the current application.

Moving documentation here does not change Warehouse OS runtime behavior.
