{
  "version": "6.12.35",
  "build": "130",
  "date": "2026-09-14",
  "channel": "stable",
  "notes": "Local Save Retry Authority: managed Warehouse saves that hit the iPhone/Safari local-cache limit now run the existing safe cleanup and retry before stopping. The first storage warning is suppressed when recovery succeeds; cloud mutation still occurs only after a confirmed local cache write. Build129 operation-draft IndexedDB recovery remains active."
}
