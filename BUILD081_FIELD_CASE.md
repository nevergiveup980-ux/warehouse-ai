# Build081 Field Case

Pure Grain / PRD-0030 / 30C / Installer Return / 5 Box.

Cloud operation remains In Progress and impactApplied=false. Product identity and inventoryRecordId make this a General Stock return even if a stale legacy editor temporarily carries Unit=Foot. Expected completion effect: Product Inventory 30C 49 Box -> 54 Box.

Build081 installs return-routing wrappers only after the Operations core functions exist, preventing early hotfix load order from skipping the wrappers.
