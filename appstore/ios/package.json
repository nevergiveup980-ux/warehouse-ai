{
  "name": "runlu-warehouse-ios",
  "private": true,
  "version": "1.0.0",
  "description": "iOS packaging workspace for RUNLU Warehouse OS",
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "web:bridge": "esbuild src/native-bridge.js --bundle --format=iife --platform=browser --outfile=www/runlu-native.js",
    "web:build": "npm run web:prepare && npm run web:bridge",
    "ios:add": "npx cap add ios",
    "ios:sync": "npm run web:build && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/haptics": "^8.0.0",
    "@capacitor/ios": "8.5.0",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "esbuild": "^0.25.0"
  }
}
