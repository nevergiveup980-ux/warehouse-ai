Implementation note: Build064 is loaded from qrcode.js as an isolated hotfix layer after Build062 and Build063. It installs an early polling guard that patches applyCloudRows as soon as the main runtime defines the cloud helpers, before the delayed background Cloud Recovery begins. This timing is required because the previous recovery starts approximately 250–350 ms after Home/login while Build062's reconciliation happens later at window load/background status checks.
